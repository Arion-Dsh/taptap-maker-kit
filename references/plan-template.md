# 架构方案模板

只保留当前任务涉及的章节；没有 API、移动端或性能变化时不要输出对应空表。

## 1. 项目确认与阶段

- Maker 绑定证据：
- 当前阶段：设计 / 初始化 / 开发 / 发布预览 / 测试二维码
- 外层仓库与 `game/` submodule 状态：

## 2. 现状

| 路径/模块 | 当前职责 | 依赖或写入 | 测试 | 问题证据 |
|---|---|---|---|---|

只列最高优先级问题，并引用实际文件、调用或文档。

## 3. 目标边界

- 只展示需要保留、移动或新增的路径。
- 标明外层事实源、`game/` 交付副本及 submodule 边界。
- 机器契约必须写出实际消费者；不要新增人类可读接口说明。

## 4. API 与存储（涉及时）

| 能力/数据 | 已确认 API | 证据 | 运行端 | 负责模块 | 错误或待查证 |
|---|---|---|---|---|---|

存储选择遵守 [API 与存储](taptap-api-priority.md)，不要在模板中重新定义规则。

## 5. 模块与状态

| 模块/状态 | 保留或变更 | 唯一写入者 | 可依赖 | 禁止依赖 | 验证 |
|---|---|---|---|---|---|

## 6. 手机 UI（涉及时）

按 [模块架构](module-architecture.md) 说明 UI Layer 根、触控坐标和验证；不要在方案中另造一套安全区规则。

## 7. 性能（涉及时）

| 场景 | 当前基线 | 瓶颈 | 改动 | 目标/阈值 | 验证 |
|---|---:|---|---|---:|---|

按 [性能](performance.md) 说明可见候选、离屏绘制、更新降频和权威逻辑例外。

## 8. 实施阶段

每阶段写明目标、文件、保留入口、验证、风险和回滚点；每阶段结束时 `game/` 仍可运行、可测试。

发布与二维码按 [Maker 生命周期](maker-workflow.md) 执行。

## 9. 非目标

列出本轮不做的玩法重写、素材重做、无关重命名，以及未经用户要求的预览或发布动作。
